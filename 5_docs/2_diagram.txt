
                               CREATIVES (20)
                                  |  (HTTPS)
                                  v
                           React SPA (browser)
                                  |
                                  v
                    FastAPI Web / API Gateway
                       |        |        |
                       |        |        +--> GET /api/events (SSE/WS)
                       |        +------------> Notify Service
                       v
                   Prompt Service
                       |
                       | enqueue
                       v
                     Redis  (Message Queue + State)
                       |
                       v
                    Orchestrator
                       |                              |    \ dispatch
                       v     v
               LLM Registry   GPU Workers (x10)
                       |           |
                       |           v
                       +--------> Artifact Store (S3)

Legend: Web/API serves SPA + public endpoints; Prompt enqueues; Orchestrator
schedules; Workers generate; Registry provides model manifests/weights.
